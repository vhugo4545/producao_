# Copie este arquivo para .env e preencha com suas configurações

# MongoDB local:
MONGO_URI=mongodb+srv://vitununesmoreira:Vhnm18842241@duli.8flnq.mongodb.net/proposta-db?retryWrites=true&w=majority&appName=Duli

# MongoDB Atlas (nuvem) — substitua pela sua connection string:
# MONGO_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/ulhoa_producao?retryWrites=true&w=majority

# Porta do servidor (padrão: 3001)
PORT=3001

# Origem permitida no CORS (URL do frontend; * = qualquer origem)
CORS_ORIGIN=*
